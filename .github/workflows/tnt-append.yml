name: Daily TnT → Sheets (append)

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  append:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - run: npm install

      - name: Write credentials file
        env:
          GOOGLE_SERVICE_ACCOUNT_BASE64: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          echo "$GOOGLE_SERVICE_ACCOUNT_BASE64" | base64 -d > /tmp/gcloud-key.json
          echo "Credentials: $(wc -c < /tmp/gcloud-key.json) bytes"

      - name: Trello → Sheets (Actions + Cards)
        env:
          TRELLO_API_KEY:        ${{ secrets.TRELLO_API_KEY }}
          TRELLO_TOKEN:          ${{ secrets.TRELLO_TOKEN }}
          TRELLO_BOARD_IDS:      ${{ secrets.TRELLO_BOARD_IDS }}
          GOOGLE_SPREADSHEET_ID: ${{ secrets.GOOGLE_SPREADSHEET_ID }}
        run: |
          mkdir -p exports
          curl -s "https://api.trello.com/1/boards/${TRELLO_BOARD_IDS}?key=${TRELLO_API_KEY}&token=${TRELLO_TOKEN}&actions=all&actions_limit=1000&cards=all&members=all&lists=all" \
            -o exports/board_today.json
          node scripts/json-to-sheets.js exports/board_today.json --mode=append

      - name: GoTo Connect → Sheets (Calls)
        env:
          GOTO_CLIENT_ID:        ${{ secrets.GOTO_CLIENT_ID }}
          GOTO_CLIENT_SECRET:    ${{ secrets.GOTO_CLIENT_SECRET }}
          GOTO_PAT:              ${{ secrets.GOTO_PAT }}
          GOTO_ACCOUNT_KEY:      ${{ secrets.GOTO_ACCOUNT_KEY }}
          GOOGLE_SPREADSHEET_ID: ${{ secrets.GOOGLE_SPREADSHEET_ID }}
        run: node scripts/fetch-goto.js
